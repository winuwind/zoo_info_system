version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: zoo_postgres
    environment:
      POSTGRES_DB: zoo_info_services_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Pasha_S_7492
    ports:
      - "5432:5432"
    volumes:
      - zoo_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  zoo_data: